# ヨーロッパ卒業旅行 2026

`index.html` をGitHub Pagesで公開する旅行しおりです。

## 自動アップロード

PowerShellで次を実行すると、ファイルを保存するたびにGitHubへ自動でコミット・プッシュします。

```powershell
powershell -ExecutionPolicy Bypass -File .\auto-upload.ps1
```

監視を終了するには `Ctrl+C` を押します。
